{
    "tasks": [
        {
            "type": "shell",
            "label": "Build GameBoy",
            "command": "source set_env.sh && make clean && make ",
            "options": {
                "cwd": "${workspaceFolder}",
            },
            "group": "build"
        }
    ],
    "version": "2.0.0"
}